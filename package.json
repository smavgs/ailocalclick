{
  "name": "ailocalclick",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Find Ollama models, save a private list, and copy one command to run them locally.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "social:generate": "node scripts/generate_social_cards.mjs",
    "sync": "./scripts/sync_catalog.sh",
    "test:data": "python3 -m unittest discover -s tests -p 'test_*.py'",
    "verify": "./scripts/verify.sh"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@types/node": "26.2.0",
    "astro": "7.2.4",
    "sharp": "^0.35.3",
    "typescript": "6.0.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  }
}
