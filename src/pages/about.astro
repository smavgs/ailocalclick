---
import BaseLayout from "../layouts/BaseLayout.astro";
import { catalog } from "../lib/catalog";

const basePath = import.meta.env.BASE_URL === "/" ? "" : import.meta.env.BASE_URL.replace(/\/$/, "");
const href = (path: string) => `${basePath}${path}`;
---

<BaseLayout
  title="Data, scope, and safety — ailocal.click"
  description="How ailocal.click sources model data, protects saved lists, and hands off visible Ollama commands."
  canonicalPath="/about/"
  pageClass="about-page"
>
  <article class="about-shell">
    <header class="about-hero">
      <p class="eyebrow">Transparent by default</p>
      <h1>Know what happens after every click.</h1>
      <p>Find a model, save it privately, or copy one command to download and run it.</p>
    </header>

    <section class="about-row">
      <div><span>01</span><h2>One official catalog source</h2></div>
      <div>
        <p>The directory follows <a href={catalog.source.url}>Ollama’s official library sorted by newest</a> and refreshes automatically.</p>
        <p>Community/user namespaces form an open-ended search surface and are not represented as a supposedly complete list.</p>
      </div>
    </section>

    <section class="about-row">
      <div><span>02</span><h2>Fresh catalog, precise details</h2></div>
      <div>
        <p>The directory refreshes every six hours. Pull totals and update labels can still change between refreshes.</p>
        <p>Exact variants, licenses, context lengths, modalities, and file sizes must be confirmed on the linked Ollama model page.</p>
      </div>
    </section>

    <section class="about-row">
      <div><span>03</span><h2>Copy first, run when ready</h2></div>
      <div>
        <p>Selecting a model copies its visible <code>ollama run …</code> command and opens simple instructions for macOS, Windows, or Linux.</p>
        <p>The website cannot install Ollama, open Terminal, paste, or execute commands. You choose when to paste the command and press Enter.</p>
      </div>
    </section>

    <section class="about-row">
      <div><span>04</span><h2>Private account saves</h2></div>
      <div>
        <p>Saving requires an account. Signed-in users keep a private synchronized list with selected tags, notes, and optional hardware preferences.</p>
        <p>Each account can access only its own profile and saved-model records. Search, filters, remove, clear, and export controls are available in <a href={href("/saved/")}>My models</a>.</p>
      </div>
    </section>

    <section class="about-row">
      <div><span>05</span><h2>Open for inspection</h2></div>
      <div>
        <p><a href="https://github.com/smavgs/ailocalclick">Read the source on GitHub ↗</a></p>
        <p>Links to Ollama and GitHub are external and follow those services’ own policies.</p>
      </div>
    </section>
  </article>
</BaseLayout>
